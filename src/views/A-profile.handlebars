<div class="container">
    <div class="row"></div>
    <div class="col-md-5 mx-auto">
        <div id="first">
            <div class="form-group">
                <br>
                <h2> Perfil Usuario </h2>
                <br>
                <tbody></tbody>
                 
                    <tr>
                        <h3>Name: {{userSession.passport.user.first_name}} {{userSession.passport.user.last_name}}</h3>
                        <h4>Email: {{userSession.passport.user.email}}</h4>
                        <h4>Role: {{userSession.passport.user.role}}</h4>
                    </tr>
                    
              </tbody>
                 <form action="/products" method="get" class="center"> <button type="submit"
                    class=" btn btn-block mybtn btn-primary tx-tfm">Seguir Navegando</button> </form>
                <br>
            </div>

        </div>
    </div>
</div>
</div>